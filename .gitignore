node_modules/
dist/
build/
coverage/
*.log
.env
.env.local
.DS_Store
out/
cache/
broadcast/
.turbo/
*.tsbuildinfo
.vercel
.env*
.flytoken

# Foundry (contract tests)
packages/contracts/lib/
packages/contracts/out/
packages/contracts/cache/
e2e/.qa-key
# BIP-39 phrase behind the four staking seats — the only thing that can move
# their funds. Never commit it.
packages/contracts/.seat-mnemonic
docs/preview/
simulation/out/
# throwaway local scripts (session probes, one-off checks)
*.tmp.mjs
*.tmp.mts
*.tmp.ts

# Claude Code personal settings (per-user permissions — never shared)
.claude/settings.local.json
dump.rdb
